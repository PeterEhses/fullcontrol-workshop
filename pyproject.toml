[project]
name = "fullcontrol-workshop"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fullcontrol>=0.1.2",
    "marimo>=0.18.4",
]

[tool.uv.workspace]
members = [
    "trial",
]
