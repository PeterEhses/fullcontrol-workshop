# 08 · Studio

**Day 4 · rest of the day**

## The move

Nothing new is taught. `template.py` has the printer profile, preview, build-volume check
and export already wired; the generation cell is empty.

## Do

Build something that uses at least one quirk from day 3 deliberately.

Export the G-code. Print it if there's a machine.

The template's closing cell lists everything you have available, including the mid-path
objects (`fc.ExtrusionGeometry`, `fc.Printer`, `fc.Extruder`) if you want flow, speed or
extrusion to change partway through.

---

## For whoever is running the closing session

This is not a review. Nobody is being marked and nothing is finished.

Run it like this:

- **Describe before interpreting.** First round is only what people can see. No "it looks
  like a…" yet, and definitely no "it works / it doesn't".
- **No quality talk in the first pass.** Once the word "good" is in the room the
  conversation narrows and doesn't widen again.
- **Talk about the path, not the person.** "This section leans out and catches" — not
  "you were ambitious here".
- **Equal airtime.** Including for the ones who'd rather not.
- **End on the next experiment, not a summary.** Everyone leaves with one thing they want
  to try, not a verdict on what they made.

That last point is the one that matters and the one that's easiest to drop when the
session runs late. The objects are unfinished on purpose. Let them stay that way.
